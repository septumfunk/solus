val my_obj = {
    x = 89
    y = 299
};
val meta = {
    inner = my_obj,
    _call = [self](string) {
        io.println(str(string) + ": " + str(self.inner.x + self.inner.y));
    }
};
obj.usemeta(my_obj, meta);
my_obj("wow");